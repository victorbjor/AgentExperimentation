def test_func(param1: str, param2: str) -> int:
    """Just a silly test function"""
    return 5


def get_current_weather(city: str) -> str:
    """Fetches current whether from API"""
    return 'Cloudy'


def is_raining_in(city: str) -> bool:
    """Fetches current whether from API and returns a bool corresponding to if it is currently raining in input city"""
    return False

